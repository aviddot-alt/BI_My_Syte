[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect for Netlify Identity (needed for Decap CMS)
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
